Add project screenshots here, named exactly:

    spendsnap.jpg / .jpeg / .png / .webp
    restrocloud.jpg / .jpeg / .png / .webp
    student-management.jpg / .jpeg / .png / .webp

Any of the four extensions works for each -- the site automatically
tries each one in turn and uses whichever file it finds. You only need
to get the base name right (spendsnap / restrocloud / student-management),
not the extension.

Recommended: 1200x900px (4:3), a clean screenshot of the app's main
screen/dashboard.

Until these exist, each project card automatically falls back to its
icon -- nothing breaks either way.
